package v3;

/**
 * Tipo de dados representando uma fracção - Versão 3.
 * <P>
 * Neste ficheiro foi colocado o tipo de dados.
 * <P>
 * A única alteração foi a qualificação da classe como {@code public},
 * necessária para que qualquer programa cliente possa declarar variáveis do
 * tipo {@code Fraction}.
 * <P>
 * (Voltar a {@link FractionCalculator v3/FractionCalculator.java}).
 * <P>
 * @author João Manuel Rodrigues
 * 2007-2017
 */

public class Fraction {
  /** Numerador */
  int num = 0;
  /** Denominador */
  int den = 1;
  // Quando criada, uma fracção é inicializada por defeito a zero (0/1)
}

