import java.util.Scanner;
public class i {
	
	public static void main (String[] args) {
		int o=0;
		int a[] = new int [1];
		String b="";
		LinkedList <Integer> c = new LinkedList <Integer>(0);
		out.println("o " + o + " a " + a[0] + " b " + b + " c " + c.elem);
		mudar(o, a, b, c);
		out.println("o " + o + " a " + a[0] + " b " + b + " c " + c.elem);
	}
	
	public static void mudar(int o, int a[], String b, LinkedList <Integer> c){
		o=2;
		a[0]=2;
		b = "ola";
		c.addFirst(2);
	}
}

